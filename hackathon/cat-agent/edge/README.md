# Edge Layer

Purpose:

- capture video / audio inputs
- run lightweight trigger logic
- emit normalized events to cloud

Minimum Week 4 scope:

- recorded clip ingestion is acceptable
- live webcam is optional
- trigger logic only needs enough fidelity to support the three demo scenarios
